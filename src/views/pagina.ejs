<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Generar Exámen</title>
    <% include partials/head %>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container p-2">
        <a class="navbar-brand" href="/">Listar examenes</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ml-auto">
            <li class="nav-item active">
              <a class="nav-link" href="/"><i class="fas fa-home"></i>Inicio</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/#about"><i class="fas fa-building"></i> Acerca de EO</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/creaexamen"><i class="fas fa-envelope"></i> Generar exámen</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/listadoexamen"><i class="fas fa-envelope"></i> Listar examenes</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>
      
    <div class="container p-5">
      <div class="row">
        <div class="col-sm-4 offset-md-4">
          <div class="card animated flipInY">
            <div class="card-header bg-dark text-white text-center">
              <h3>Exámen</h3>
            </div>
            <div class="container p-4">
            <%long=(JSON.parse(dats)).table.length%>  
            <%for (index=0; index<long; index++){%>
              <form action="/borrar/<%=(JSON.parse(dats)).table[index].id%>"  method="get">
                <div class="form-group">Nombre del examen:
                  <%= (JSON.parse(dats)).table[index].nombre;%>
                </div>
                <div class="form-group">Contenido/descripción del examen:
                  <%= (JSON.parse(dats)).table[index].contenido;%>
                </div>
                <div class="form-group">
                  <button class="btn btn-primary btn-block">
                    Borrar
                  </button>
                </div>               
              </form>
              <% 
            }%> 
            </div>
          </div>
        </div>
      </div>
    </div>
    <% include partials/footer %>
  </body>
</html>

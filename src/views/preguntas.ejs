<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <% include partials/head %>
  </head>
  <body>
    <% include partials/navigation %>
    <script src="http://js.nicedit.com/nicEdit-latest.js" type="text/javascript"></script>
    <script type="text/javascript">bkLib.onDomLoaded(nicEditors.allTextAreas);</script>
    
      <div class="container p-5">
      <div class="floating-component">
      <div class="pregunta">
      <div class="card card-body text-center"><h3>Preguntas</h3></div> 
          <%long=((JSON.parse(dats)).table.length)-1;
          if ((JSON.parse(dats)).table[long].opciones == 'Varias opciones'){%> 
          <form method="POST" action="/otras_respuesta/<%=(JSON.parse(dats)).table[long].id%>" class="card card-body">
              <div class="form-group">
                <div class="floating-form-group">
                  <input class="floating-form-control" name="seccion" type="text" value="<%= (JSON.parse(dats)).table[long].seccion;%>">
                  <span class="highlight"></span>
                </div>
              </div>
              <div class="form-group">
              <div class="floating-form-group">
                  <input class="floating-form-control" name="title" type="text" value="<%= (JSON.parse(dats)).table[long].title;%>">
                  <span class="highlight"></span>
              </div>
              </div>
              <div class="form-group">
              <div class="floating-form-group">
                <input class="floating-form-control" name="descripcion" type="text" value="<%= (JSON.parse(dats)).table[long].descripcion;%>">
                <span class="highlight"></span>
              </div>
              </div>
              <div class="form-group">
              <div class="floating-form-group">
                <p>Tipo de respuesta a la pregunta:</p>
                <p style="text-align:center"><%= (JSON.parse(dats)).table[long].opciones;%></p>
                <span class="highlight"></span>
              </div>
              </div>
              <div class="form-group">
              <%long1=(JSON.parse(dats)).table[long].titleopcion.length;%> 
              <%for (index1=0; index1<long1; index1++){%>
                 <input type="checkbox" id="checkboxtitleopcion" name="checkboxtitleopcion" value="<%= (JSON.parse(dats)).table[long].titleopcion[index1];%>">
                 <span class="highlight"></span>
                 <label for="checkboxtitleopcion"><input class="floating-form-control" name="titleopcion" type="text" class="form-control" size='30' value="<%= (JSON.parse(dats)).table[long].titleopcion[index1];%>" ></label><br>
              <%}%> 
              <input type="checkbox" id="checkboxtitleopcion" name="checkboxtitleopcion" value="ultima opción">
              <span class="highlight"></span>
              <label for="checkboxtitleopcion"><input class="floating-form-control" name="titleopcion" type="text" class="form-control" value="Añadir opción" size='30' autofocus required></label>
              <input type="submit" name="resotro" onclick="alert('¡Recuerde: antes de guardar marcar la opción/es correcta/s!');" value="Añadir respuesta 'Otro'" class="floating-select">
              </div>       
             <br>
             <div class="form-group">
               <label><h10>Antes de guardar marcar la opción/es correcta/s</h10></label><br>
               <div class="contenedor">
                <button name="resotro" onclick="alert('Pregunta con respuesta varias opciones ¡guardada con éxito!');" value="Guardar" class="centrado">Guardar</button>
              </div>
             </div>
          </form>
          <%}%>   
          
  </div></div></div>
  <% include partials/footer %>   
  </body>
 
</html>

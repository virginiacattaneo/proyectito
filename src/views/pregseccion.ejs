<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Listado preguntas por Secciones</title>
    <% include partials/head %>
  </head>
  <body>
    <% include partials/navigation %>
    
    <div class="container p-5">
      <div class="row">
        <div >
          <div >
            <div >
              <h2>Listado de preguntas por secciones</h2>
              <table class="pregseccion">
                <tr>
                  <th class="th"><p>Seccion probando si actualiza</p></th>
                  <th class="th"><p>Preguntas</p></th> 
                </tr>
                <%long=(JSON.parse(dats)).table.length%>  
                <%for (index=0; index<long; index++){%>       
              </table>
              <tr>
                <td class="td"><p> <%= (JSON.parse(dats)).table[index].seccion;%></p></td>
                </tr>


            </div>
          </div>
        </div>
      </div>
    </div>
    <% include partials/footer %>
  </body>
</html>

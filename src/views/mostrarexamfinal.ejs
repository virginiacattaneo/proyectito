<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <% include partials/head %>
  </head>
  <body>
    <% include partials/navigation %>
    <div class="fullscreen">
    <div class="container p-5">
    <div class="floating-component">
    <div class="pregunta">
    <script src="http://js.nicedit.com/nicEdit-latest.js" type="text/javascript"></script>
    <script type="text/javascript">bkLib.onDomLoaded(nicEditors.allTextAreas);</script>
  <form method="POST" action="/generarexamenfinal" class="card card-body">         
      <div class="card card-body text-center"><h3>Exámen</h3></div>
      <%long=(JSON.parse(dats)).examen.length;%>
        <%for (index=0; index<long; index++){%> 
            <div class="form-group">
                <div class="floating-form-group">
                  <p style="text-align:right"><%= (JSON.parse(dats)).examen[index].seccion;%></p>
                </div>
            </div>
            <div class="form-group">
                <div class="floating-form-group">
                  <label><%= (JSON.parse(dats)).examen[index].title;%></label><p>Asignar Puntaje:<input class="form-control" type="number" style="text-align:right" name="puntaje" id="number" min="1" max="100" step="1" placeholder="1"></p>
                </div>
            </div>
              <div class="form-group">
                <div class="floating-form-group">
                  <label><%= (JSON.parse(dats)).examen[index].descripcion;%></label>
                </div>
              </div>             
              <div class="form-group">
              <% if ((JSON.parse(dats)).examen[index].opciones == 'Varias opciones'){%>
                    <%long1=(JSON.parse(dats)).examen[index].titleopcion.length;%> 
                    <%for (index1=0; index1<long1; index1++){%>
                        <input type="checkbox" id="checkboxtitleopcion" name="checkboxtitleopcion" value="<%= (JSON.parse(dats)).examen[index].titleopcion[index1];%>">
                        <label><%= (JSON.parse(dats)).examen[index].titleopcion[index1];%></label><span class="highlight"></span><br>
                    <%}%> 
                <%}
                if ((JSON.parse(dats)).examen[index].opciones == 'Respuesta corta'){%>
                         <div class="form-group">
                           <div class="floating-form-group">
                          <textarea class="floating-form-control floating-textarea" name="respuestacorta"></textarea>
                          <span class="highlight"></span>
                          </div>
                        </div>  
                    <%}%>
               </div>   
        <%}%>    
         
          <div class="form-group">
            <input type="submit" name="guardar" onclick="this.setAttribute('value', this.value);" value="Agregar más secciones" class="floating-select">
          </div>
          <div class="form-group">
            <input type="submit" name="guardar" onclick="this.setAttribute('value', this.value);" value="Guardar exámen" class="floating-select">
          </div>
                  
  </div></div></div></div>
  </form>
  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js" integrity="sha384-ZMP7rVo3mIykV+2+9J3UJ46jBk0WLaUAdn689aCwoqbBJiSnjAK/l8WvCWPIPm49" crossorigin="anonymous"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js" integrity="sha384-ChfqqxuZUCnJSK3+MXmPNIyE6ZbWh2IMqE241rYiqJxyMiZ6OW/JmZQ5stwEULTy" crossorigin="anonymous"></script>
  </body>
</html>

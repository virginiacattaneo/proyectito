<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
  </head>
  <body>
    <div class="pregunta">
    <script src="http://js.nicedit.com/nicEdit-latest.js" type="text/javascript"></script>
    <script type="text/javascript">bkLib.onDomLoaded(nicEditors.allTextAreas);</script>
  <form method="POST" action="/" class="card card-body">         
      <div class="card card-body text-center"><h3>Exámen</h3></div>
        <p>Estudiante DNI:<%= (JSON.parse(dats)).alumnoasignado;%></p>  
        <%long=(JSON.parse(dats)).examenes.length;%>
        <%for (index=0; index<long; index++){%> 
            <p>Sección:<%= (JSON.parse(dats)).examenes[index].seccion;%></p>
            <label>Título:<%= (JSON.parse(dats)).examenes[index].title;%></label><p>Puntaje:<%= (JSON.parse(dats)).examenes[index].puntaje;%></p>
            <label>Descripción: <%= (JSON.parse(dats)).examenes[index].descripcion;%></label>
            <% if ((JSON.parse(dats)).examenes[index].opciones == 'Varias opciones'){%>
                <%long1=(JSON.parse(dats)).examenes[index].titleopcion.length;%> 
                <%for (index1=0; index1<long1; index1++){%>
                        <input type="checkbox" id="checkboxtitleopcion" name="checkboxtitleopcion" value="<%= (JSON.parse(dats)).examenes[index].titleopcion[index1];%>">
                        <label><%= (JSON.parse(dats)).examenes[index].titleopcion[index1];%></label><br>
                        <%}%> 
                <%}
                  if ((JSON.parse(dats)).examenes[index].opciones == 'Respuesta corta'){%> 
                        <textarea class="floating-form-control floating-textarea" name="respuestacorta"></textarea><br>
                <%}%>     
          <%}%>
          <!-- <input type="submit" name="guardar" onclick="this.setAttribute('value', this.value);" value="Enviar" class="floating-select">-->
         
    </form>
    </div>
  
  </body>
</html>
